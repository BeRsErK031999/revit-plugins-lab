# Plugins

Each folder in this directory should represent one Revit add-in product or prototype.

Recommended plugin layout:

```text
plugin-name/
  README.md
  src/
  tests/
  docs/
  installer/
```

Start products here while they are experimental. Move mature products to separate repositories only when release, access, or support boundaries require it.
