# План работ по BIM-инструментам

## Исходный контекст

Первый BIM-набор в проекте уже реализован внутри существующей Revit-вкладки `TrueBIM`. Он закрывает задачи проверки модели, параметров и администрирования:

- `Цвета по параметрам`
- `Копирование параметров`
- `Рабочие наборы`
- `ParaManager`

Новое ТЗ добавляет отдельный пакет `БИМ-оформление`: размеры, марки, штампы, виды, выпуск, библиотека семейств и координация. Чтобы не переносить существующие рабочие команды, текущий план сохраняет хост-вкладку `TrueBIM` и добавляет новые BIM-панели внутри неё.

## Пакет БИМ-Оформление

Статус: каркас добавлен, рабочие MVP `Диспетчер семейств`, `Оформить штамп`, `Автомарки`, `Оси 2D/3D`, `Виды дверей/окон` и `Отчёт коллизий` реализованы. `Импорт таблиц` добавлен как отдельный инструмент на панели `БИМ`: первый рабочий режим создаёт визуальную таблицу из PDF/JSON, а прямой DWG-вход пока отмечен как плановый. Отдельные инструменты `Экспорт PDF/DWG` и MEP-авторазмеров удалены: выпуск листов выполняется через модуль `Печать`, а MEP-размеры больше не входят в TrueBIM.

| Инструмент | Панель | Что уже есть в проекте | Чего не хватает | Следующий шаг |
| --- | --- | --- | --- | --- |
| `Диспетчер семейств` | `Библиотека` | Готов MVP+: папки библиотек, рекурсивный скан `.rfa`, дерево `Библиотека -> Категория -> Семейство -> Тип`, поиск, фильтр по категории, JSON-кэш, избранное, история, ручное и пакетное чтение реальной категории/типов/параметров из `.rfa`, Explorer actions, загрузка и штатное размещение первого/выбранного загруженного типа. | Нет миниатюр и загрузки конкретного типа из `.rfa` без полной загрузки семейства. | Дальше идти по `plugins/truebim/docs/family-manager-roadmap.md`: thumbnails, parameter filters и type catalog. |
| `Оформить штамп` | `Оформление` | Готов MVP+: выбор листов, правила `Лист/Штамп -> параметр -> источник`, выпадающий список записываемых параметров листов/штампов, формулы с токенами листа/даты/параметров, предпросмотр, запись в `Transaction`, JSON-профиль и CSV-отчёт. Переиспользуется `SheetCollectorService`. | Нет массового импорта профилей и записи в несколько title block instances на одном листе. | После ручной проверки решить, нужны ли шаблоны профилей или перейти к `Автомарки`. |
| `Автомарки` | `Оформление` | Готов MVP+: активный 2D-вид, выбор категорий, тип марки по умолчанию и отдельный тип марки для каждой категории, проверка существующих марок на виде, safe preview, настраиваемое X/Y смещение марки по активному виду, постановка через `IndependentTag.Create` внутри `Transaction`, JSON-профиль и CSV-отчёт. | Нет раскладки без пересечений, смещений по отдельным категориям, проверки совместимости выбранного типа марки до применения, работы со связанными файлами и заблокированными 3D-видами. | После ручной проверки решить, нужны ли category-specific offset/anti-overlap правила или перейти к `Оси 2D/3D`. |
| `Оси 2D/3D` | `Виды` | Готов MVP+: активный 2D-вид, сбор видимых осей и уровней, preview текущего режима End0/End1, переключение выбранных концов между `Model` и `ViewSpecific` через `SetDatumExtentType`, opt-in распространение текущих экстентов на совместимые виды через `PropagateToViews`, JSON-профиль и CSV-отчёт. | Нет выравнивания геометрии концов по crop/границе вида, пакетного выбора конкретных целевых видов и правил смещения bubble. | После ручной проверки решить, нужны ли align/crop-инструменты и выбор конкретных propagation views или перейти к `Виды дверей/окон`. |
| `Виды дверей/окон` | `Виды` | Готов MVP+: активный план, видимые двери и опционально окна, выбор типа elevation и шаблона вида, preview имён `BIM_Opening_*`, проверка дублей, выбор ориентации `По элементу`/`По стене`, создание `ElevationMarker`/`ViewSection`, crop box вокруг проёма, JSON-профиль и CSV-отчёт. | Нет пакетного размещения на листах, нескольких направлений на один проём, витражей, связанных файлов и правил оформления листов. | После ручной проверки решить, нужны ли листы/мультинаправления или перейти к `Отчёт коллизий`. |
| `Отчёт коллизий` | `Координация` | Готов MVP+: импорт CSV/XML со списком коллизий, фильтр, статусы и комментарии, локальное JSON-состояние по ключу model + clash, resolve ElementId, переход в служебный 3D-вид `BIM_Clash_Report_3D`, section box, selection и подсветка найденных элементов. | Нет BCF-синхронизации, пакетных снимков, marker geometry, очистки старых overrides и расширенного маппинга форматов XML. Плагин больше не ищет коллизии сам. | После ручной проверки расширять только импорт/навигацию: BCF, Navisworks XML mapping, пакетные снимки и dockable pane. |
| `Импорт таблиц` | `БИМ` | Готов первый рабочий срез: кнопка `Импорт таблиц`, WPF-окно, выбор PDF/JSON, локальный PDF worker или JSON-модель, предпросмотр таблицы, выбор режима, `Drafting Table Mode` через `ExternalEvent`, BIM Schedule dry-run с сопоставлением колонок и диагностикой ключей. | Нет прямого DWG parser, записи значений в реальные элементы для BIM Schedule Mode, Key Schedule Mode и ручной QA в Revit после локального deploy. | Следующий инженерный шаг - BIM Schedule write-flow; для DWG сначала выбрать стратегию: конвертация DWG в PDF/геометрию или отдельный backend parser. |

## Каркас Пакета

Статус: готово первым срезом.

Состав:

- добавлены панели `Оформление`, `Виды`, `Координация`, `Библиотека`;
- добавлены кнопки `Автомарки`, `Оформить штамп`, `Оси 2D/3D`, `Виды дверей/окон`, `Отчёт коллизий`, `Диспетчер семейств`;
- для каждой кнопки создан отдельный `IExternalCommand`;
- общий scaffold-window показывает профиль будущего инструмента, сценарий, следующий срез и кнопки `Предпросмотр`, `Выполнить`, `Закрыть`;
- добавлены общие сервисы `JsonSettingsStorage`, `ReportService`, `CsvExportService`;
- первый срез не создает, не удаляет и не изменяет элементы модели Revit.

Manual QA:

1. Перезапустить Revit после установки новой сборки.
2. Открыть вкладку `TrueBIM`.
3. Проверить панели `Оформление`, `Виды`, `Координация`, `Библиотека`.
4. Нажать каждую новую кнопку и убедиться, что открывается отдельное окно инструмента.
5. Нажать `Предпросмотр`, затем `Выполнить`; статус должен обновиться, модель Revit не должна измениться.
6. Проверить, что файл настроек создается в `%AppData%/TrueBIM/BimTools/<tool>/settings.json`.
7. Закрыть окна и убедиться, что Revit продолжает работать.

## Готовность

| Срез | Статус | Комментарий |
| --- | --- | --- |
| 0. Каркас пакета `БИМ-оформление` | Готово | Добавлены новые панели, 8 команд-заглушек, общий JSON storage, report и CSV-сервис. |
| 0.1. Экспорт PDF/DWG | Удалено | Отдельная команда снята с панели `Выпуск`; экспорт PDF/DWG/DXF/DWF выполняется через модуль `Печать`. |
| 0.2. Диспетчер семейств MVP+ | Готово | Реализовано окно `Диспетчер семейств`: папки библиотек, скан `.rfa`, дерево структуры, поиск, категории, ручное/пакетное обновление кэша типов и параметров из family-документа, избранное, история, Explorer actions, загрузка и запуск штатного размещения. |
| 0.3. Оформить штамп MVP+ | Готово | Реализовано окно `Оформить штамп`: выбор листов, правила параметров листа/штампа, формулы, предпросмотр, применение и CSV-отчёт. |
| 0.4. Автомарки MVP+ | Готово | Реализовано окно `Автомарки`: категории активного 2D-вида, проверка существующих марок, выбор типа марки, X/Y смещение, предпросмотр, создание `IndependentTag` и CSV-отчёт. |
| 0.5. Оси 2D/3D MVP+ | Готово | Реализовано окно `Оси 2D/3D`: оси/уровни активного вида, End0/End1, режим 2D/3D, применение `SetDatumExtentType`, opt-in `PropagateToViews` и CSV-отчёт. |
| 0.6. MEP-авторазмеры | Удалено | Команда, ribbon-кнопка, окно, сервисы и тесты MEP-авторазмеров удалены из TrueBIM. |
| 0.7. Виды дверей/окон MVP+ | Готово | Реализовано окно `Виды дверей/окон`: двери/окна активного плана, elevation type/template, ориентация по элементу или стене-основе, проверка дублей, `ElevationMarker`, crop box и CSV-отчёт. |
| 0.8. Отчёт коллизий MVP+ | Готово | Реализовано окно `Отчёт коллизий`: импорт CSV/XML, resolve элементов основной модели и связанных ElementId, статусы/комментарии, JSON-состояние, служебный 3D section box, selection и подсветка. |
| 0.9. Импорт таблиц PDF/JSON | Частично готово | PDF/JSON preview и `Drafting Table Mode` работают; BIM Schedule Mode доступен как dry-run сопоставления; DWG честно помечен как плановый вход через предварительный экспорт в PDF. |
| 1. Ribbon и UI-каркас | Готово | Кнопки и окна-каркасы добавлены, commit `6c22af6`. |
| 2. Копирование параметров | Готово | Реализованы выбор исходного элемента, параметров, получателей, копирование внутри `Transaction` и итоговый отчёт. |
| 3. Рабочие наборы | Готово | Реализован CSV-only MVP: выбор файла, предпросмотр, валидация, включение worksharing по подтверждению и создание worksets. |
| 4. Цвета по параметрам | Готово | Реализован MVP: категории/параметры активного вида, уникальные значения, фильтры `BIM_F_`, графические overrides и очистка активного вида. |
| 5. ParaManager MVP | Готово | Реализован CSV-only MVP: импорт shared parameters в проект, предпросмотр, валидация, привязка к категориям и отчёт. |
| 6. ParaManager экспорт параметров | Готово | Реализован экспорт текущих project parameters в CSV из вкладки `Параметры проекта`. |
| 7. Релизная сборка BIM-срезов | Готово | Пройдены `format`, Release build/test и сборка `TrueBIM-Setup.exe` для Revit 2019-2025. |

## Задача 0.1: Экспорт PDF/DWG

Статус: удалено.

Отдельная команда `Экспорт PDF/DWG` снята с панели `Выпуск`, а код `BatchExport` удалён. Экспорт листов в PDF/DWG/DXF/DWF выполняется через модуль `Печать`, чтобы не держать два пересекающихся пользовательских сценария.

## Задача 0.2: Диспетчер Семейств

Статус: готово.

Цель: дать безопасный браузер библиотек `.rfa`, который помогает найти семейство, сохранить избранное/историю и загрузить его в текущий проект без редактирования family-документов.

Фактически реализовано:

- команда `FamilyManagerCommand` запускается из панели `Библиотека`;
- окно `FamilyManagerWindow` заменяет заглушку `Диспетчер семейств`;
- можно добавить и удалить папки библиотек;
- сканер рекурсивно ищет `.rfa` в включенных папках и кэширует результат в `%AppData%/TrueBIM/BimTools/family-manager/settings.json`;
- для выбранной папки можно нажать `Метаданные папки`: инструмент последовательно открывает семейства из этой папки, обновляет категорию/типы/параметры и сохраняет JSON-cache;
- дерево структуры показывает библиотеки, категории, семейства и типы; выбор семейства/типа синхронизируется с таблицей и правой панелью;
- есть поиск по имени, категории и пути;
- первичные категории определяются эвристически по имени файла/папок, без открытия `.rfa`;
- для выбранного семейства можно нажать `Обновить метаданные`: инструмент открывает `.rfa` как family-документ, читает реальную категорию, список типов и параметры каждого типа, закрывает файл без сохранения и сохраняет результат в JSON-кэш;
- повторное сканирование сохраняет прочитанные метаданные, если `.rfa` не изменился по размеру и времени изменения;
- есть фильтр категорий и режим показа только избранного;
- избранное и история загрузок сохраняются в JSON;
- если семейство уже загружено, инструмент спрашивает, перезаписать файлом библиотеки или использовать существующее семейство;
- загрузка семейства выполняется через Revit API внутри `Transaction`;
- правая панель показывает параметры выбранного типа из кэша метаданных;
- поиск учитывает имена, значения и формулы параметров из metadata-cache;
- в дереве и таблице есть контекстные действия `Открыть в Explorer` и `Скопировать путь`;
- кнопка `Загрузить и разместить` активирует первый, выбранный уже загруженный или выбранный из кэша `.rfa` тип и запускает штатное размещение Revit через `PostRequestForElementTypePlacement`.

Manual QA:

1. Установить свежий `TrueBIM-Setup.exe` или выполнить local deploy после закрытия Revit.
2. Открыть проект Revit.
3. Вкладка `TrueBIM` -> панель `Библиотека` -> `Диспетчер семейств`.
4. Добавить папку с несколькими `.rfa` файлами и нажать `Сканировать`.
5. Проверить список семейств, поиск по имени, фильтр категории и переключатель `Избранное`.
6. Проверить дерево слева: выбор семейства и типа должен выбирать ту же строку и тип в правой панели.
7. Открыть контекстное меню дерева/таблицы, проверить `Открыть в Explorer` и `Скопировать путь`.
8. Выбрать папку библиотеки, нажать `Метаданные папки`, подтвердить пакетное обновление и проверить итоговый статус.
9. Выбрать семейство, нажать `Обновить метаданные`, проверить реальную категорию, количество типов, список `Типы в файле/проекте` и таблицу параметров выбранного типа.
10. Закрыть и снова открыть окно; прочитанные типы и категория должны восстановиться из JSON-кэша.
11. Повторно нажать `Сканировать`; если `.rfa` не менялся, прочитанные метаданные должны сохраниться.
12. Добавить одно семейство в избранное, закрыть и снова открыть окно; избранное должно восстановиться.
13. Выбрать семейство, нажать `Загрузить` и проверить появление семейства в Project Browser.
14. Повторить для уже загруженного семейства и проверить диалог подтверждения перезаписи.
15. Нажать `Загрузить и разместить`; Revit должен перейти в штатный режим размещения типа семейства.
16. Проверить, что история загрузок сохраняется после перезапуска окна.

## Задача 0.3: Оформить Штамп

Статус: готово.

Цель: безопасно заполнять выбранные параметры листов и размещённых на них title block instances по настраиваемым правилам с предпросмотром до записи.

Фактически реализовано:

- команда `TitleBlockFillCommand` запускается из панели `Оформление`;
- окно `TitleBlockFillWindow` заменяет заглушку `Оформить штамп`;
- список листов берётся через существующий `SheetCollectorService`;
- для каждого листа показывается статус найденного штампа;
- профиль правил хранится в `%AppData%/TrueBIM/BimTools/title-block-fill/settings.json`;
- правило задаёт цель `Лист` или `Штамп`, имя параметра, источник значения и дополнительное значение;
- поддержаны источники `Статический текст`, `Дата`, `Номер листа`, `Имя листа`, `Параметр проекта`, `Параметр листа`, `Формула`;
- источник `Формула` поддерживает токены `{SheetNumber}`, `{SheetName}`, `{Date:yyyy-MM-dd}`, `{ProjectParameter:...}` и `{SheetParameter:...}`;
- `Предпросмотр` показывает текущее и новое значение, read-only параметры, отсутствующие параметры и неподдержанные storage types;
- `Применить` записывает значения внутри одного Revit `Transaction`;
- ошибки отдельных листов или параметров попадают в отчёт и не прерывают весь инструмент;
- отчёт можно сохранить в CSV UTF-8 with BOM.

Manual QA:

1. Установить свежий `TrueBIM-Setup.exe` или выполнить local deploy после закрытия Revit.
2. Открыть проект Revit с листами и размещёнными штампами.
3. Вкладка `TrueBIM` -> панель `Оформление` -> `Оформить штамп`.
4. Проверить список листов, фильтр и колонку `Штамп`.
5. На вкладке `Правила` добавить правило для безопасного текстового параметра листа, например `Комментарии`, со статическим текстом.
6. Добавить правило с источником `Формула`, например `{SheetNumber}_{SheetName}_{Date:yyyy-MM-dd}`, и проверить вычисленное значение в предпросмотре.
7. Добавить правило для параметра штампа, который точно существует в title block family.
8. Нажать `Предпросмотр` и проверить текущие/новые значения на вкладке `Отчёт`.
9. Нажать `Применить`, подтвердить запись и проверить отчёт по записанным/пропущенным/ошибочным строкам.
10. Проверить значения параметров листа и штампа в Revit, затем сделать Undo.
11. Закрыть и снова открыть окно; профиль правил должен восстановиться из JSON.

## Задача 0.4: Автомарки

Статус: готово.

Цель: безопасно поставить марки на элементы активного 2D-вида с предпросмотром, проверкой уже существующих марок и отчётом по каждому элементу.

Фактически реализовано:

- команда `AutoTagCommand` запускается из панели `Оформление`;
- окно `AutoTagWindow` заменяет заглушку `Автомарки`;
- инструмент работает только на обычном активном 2D-виде и отклоняет шаблоны, листы, browser views и 3D-виды;
- категории собираются из model-элементов, видимых на активном виде;
- можно выбрать категории, тип марки из загруженных tag family symbols или оставить режим `Авто (по категории)`;
- профиль сохраняется в `%AppData%/TrueBIM/BimTools/auto-tags/settings.json`;
- профиль хранит смещение марки по X/Y активного вида в миллиметрах; значения ограничены диапазоном `-5000..5000`;
- `Предпросмотр` показывает ElementId, категорию, имя, число существующих марок, статус и причину пропуска;
- preview/report показывают выбранное смещение, если оно не равно нулю;
- по умолчанию включён режим `Только без марки`, который пропускает элементы с уже существующей маркой на активном виде;
- точка марки берётся из `LocationPoint`, середины `LocationCurve` или центра bounding box;
- `Поставить марки` создаёт `IndependentTag` внутри Revit `Transaction`;
- ошибки отдельных элементов попадают в отчёт и не прерывают весь инструмент;
- отчёт preview/apply можно сохранить в CSV UTF-8 with BOM.

Manual QA:

1. Установить свежий `TrueBIM-Setup.exe` или выполнить local deploy после закрытия Revit.
2. Открыть проект Revit с загруженными марками для стен, дверей или другого model-класса.
3. Открыть план, разрез или фасад с несколькими видимыми элементами.
4. Вкладка `TrueBIM` -> панель `Оформление` -> `Автомарки`.
5. Проверить список категорий и оставить одну безопасную категорию, например `Стены` или `Двери`.
6. Оставить тип `Авто (по категории)` или выбрать совместимый тип марки.
7. Нажать `Предпросмотр` и проверить строки `Готово` / `Пропущено`, особенно для элементов, у которых уже есть марки.
8. Задать небольшое смещение, например `X=150`, `Y=80`, повторить preview и проверить, что отчёт показывает выбранное смещение.
9. Выбрать 1-2 строки, нажать `Поставить марки`, подтвердить операцию и проверить появление марок на активном виде со смещением от центра элемента.
10. Проверить отчёт, сохранить CSV и открыть его в Excel/текстовом редакторе.
11. Сделать Undo в Revit, затем закрыть и снова открыть окно; профиль категорий, тип марки и смещение должны восстановиться из JSON.

## Задача 0.5: Оси 2D/3D

Статус: готово.

Цель: безопасно переключать 2D/3D экстенты осей и уровней на активном виде без распространения изменений на другие виды.

Фактически реализовано:

- команда `DatumExtentCommand` запускается из панели `Виды`;
- окно `DatumExtentWindow` заменяет заглушку `Оси 2D/3D`;
- инструмент работает только на обычном активном 2D-виде и отклоняет шаблоны, листы, browser views, schedules, legends и 3D-виды;
- можно включить или выключить обработку `Оси`, `Уровни`, `End0`, `End1`;
- целевой режим выбирается как `2D на активном виде` (`DatumExtentType.ViewSpecific`) или `3D модельные экстенты` (`DatumExtentType.Model`);
- опция `Распространить` по умолчанию выключена и при включении вызывает `PropagateToViews` только для выбранных datum-элементов;
- профиль сохраняется в `%AppData%/TrueBIM/BimTools/datum-extents/settings.json`;
- `Предпросмотр` показывает ElementId, тип datum-элемента, имя, текущий режим End0/End1, количество model/2D-кривых, число совместимых видов для propagation и причину пропуска;
- `Применить` вызывает `SetDatumExtentType` внутри Revit `Transaction`;
- если включено распространение, после обработки активного вида текущие экстенты выбранного datum-элемента распространяются на совместимые виды внутри той же `Transaction`;
- ошибки отдельных datum-элементов попадают в отчёт и не прерывают весь инструмент;
- отчёт preview/apply можно сохранить в CSV UTF-8 with BOM.

Manual QA:

1. Установить свежий `TrueBIM-Setup.exe` или выполнить local deploy после закрытия Revit.
2. Открыть проект Revit с сеткой осей и уровнями.
3. Открыть план, разрез или фасад, где видны несколько осей или уровней.
4. Вкладка `TrueBIM` -> панель `Виды` -> `Оси 2D/3D`.
5. Проверить список datum-элементов и текущие значения `End0`/`End1`.
6. Оставить только `Оси`, выбрать режим `2D на активном виде` и нажать `Предпросмотр`.
7. Выбрать 1-2 оси, нажать `Применить`, подтвердить и проверить, что выбранные концы стали 2D на активном виде.
8. Переключить целевой режим на `3D модельные экстенты`, повторить для тех же осей и проверить возврат в 3D.
9. Включить `Распространить`, нажать `Предпросмотр` и проверить колонку `Видов` для выбранных осей/уровней.
10. Применить к 1-2 безопасным datum-элементам и проверить, что совместимые виды получили те же экстенты.
11. Сохранить CSV-отчёт и открыть его в Excel/текстовом редакторе.
12. Сделать Undo в Revit, затем закрыть и снова открыть окно; профиль режима, чекбоксов и опции распространения должен восстановиться из JSON.

## Задача 0.6: MEP-авторазмеры

Статус: удалено.

Команда, ribbon-кнопка, окно, сервисы и тесты MEP-авторазмеров удалены из TrueBIM. В панели `Оформление` остаются `Автомарки` и `Оформить штамп`.

## Задача 0.7: Виды дверей/окон

Статус: готово.

Цель: безопасно создавать фронтальные elevation-виды для дверей и, опционально, окон на активном плане с предпросмотром имён, проверкой дублей и отчётом до записи.

Фактически реализовано:

- команда `OpeningViewsCommand` запускается из панели `Виды`;
- окно `OpeningViewsWindow` заменяет заглушку `Виды дверей/окон`;
- инструмент работает только на активном плане и отклоняет шаблоны, листы, 3D, schedules, legends и browser views;
- по умолчанию собираются двери на активном плане, окна можно включить отдельным чекбоксом;
- профиль сохраняется в `%AppData%/TrueBIM/BimTools/opening-views/settings.json`;
- можно выбрать `ViewFamilyType` с `ViewFamily.Elevation`, шаблон elevation-вида, масштаб, запас crop и глубину crop в миллиметрах;
- можно выбрать источник ориентации: `По элементу` использует `FamilyInstance.FacingOrientation`, `По стене` использует нормаль стены-основы и выбирает сторону, ближайшую к `FacingOrientation`;
- имя вида строится по шаблону с токенами `{ElementId}`, `{CategoryKey}`, `{Category}`, `{Family}`, `{Type}`, `{Level}` и sanitizing для Revit view name;
- `Предпросмотр` показывает ElementId, категорию, семейство, тип, уровень, будущее имя вида, выбранный источник ориентации, статус и причину пропуска;
- дубли проверяются по существующим именам видов и не создаются повторно;
- `Создать виды` создаёт `ElevationMarker`, затем `ViewSection` через `CreateElevation`, выравнивает маркер по выбранной ориентации, применяет шаблон вида и задаёт crop box вокруг bounding box элемента;
- каждый проём обрабатывается внутри отдельного `SubTransaction`, поэтому ошибка одного элемента не оставляет частично созданный вид и не прерывает весь инструмент;
- отчёт preview/apply можно сохранить в CSV UTF-8 with BOM.

Ограничения MVP:

- создаётся один фронтальный elevation на проём;
- пока нет автоматической раскладки видов на листы, нескольких направлений на один проём, витражей, linked files, пакетного выбора нескольких планов и расширенных правил именования листов;
- если для режима `По стене` стена-основа или её `LocationCurve` не найдены, инструмент откатывается к `FamilyInstance.FacingOrientation` и показывает это в preview.

Manual QA:

1. Установить свежий `TrueBIM-Setup.exe` или выполнить local deploy после закрытия Revit.
2. Открыть проект Revit с дверями на плане.
3. Открыть план, где видны несколько дверей.
4. Вкладка `TrueBIM` -> панель `Виды` -> `Виды дверей/окон`.
5. Проверить, что выбран тип фасадного вида; при необходимости выбрать шаблон elevation-вида.
6. Оставить `Ориентация = По элементу`, оставить включёнными `Двери`, нажать `Предпросмотр` и проверить имена `BIM_Opening_*`, статусы `Готово` / `Пропущено`.
7. Переключить `Ориентация = По стене`, повторить `Предпросмотр` и проверить колонку `Ориентация`; если стена-основа не найдена, строка должна показать fallback на `По элементу`.
8. Выбрать 1-2 готовые двери, нажать `Создать виды` и подтвердить операцию.
9. Проверить созданные elevation-виды в Project Browser, ориентацию вида по стене/элементу и crop вокруг проёма.
10. Включить `Окна`, повторить preview на плане с окнами.
11. Сохранить CSV-отчёт, затем сделать Undo в Revit и проверить, что профиль окна, категорий и ориентации восстанавливается из JSON.

## Задача 0.8: Отчёт коллизий

Статус: готово.

Цель: дать координатору простой инструмент импорта внешнего списка коллизий, ведения статусов и быстрого перехода к проблемному месту в 3D.

Фактически реализовано:

- команда `ClashReportCommand` запускается из панели `Координация`;
- окно `ClashReportWindow` заменяет заглушку `Отчёт коллизий`;
- кнопка `Добавить файл` импортирует CSV или XML со списком коллизий;
- CSV и XML являются входным источником коллизий, внутренний поиск коллизий удалён;
- CSV поддерживает заголовки `ClashId`, `ClashName`, `ElementId1`, `ElementId2`, `X`, `Y`, `Z`, `Status`, `Comment`;
- XML поддерживает `clash`, `clashresult`, `result`, `item`, `clashpoint` и Navisworks-like `Element ID` attributes;
- статусы нормализуются в `Open`, `In Progress`, `Resolved`, `Ignored`;
- элементы текущей модели разрешаются через `Document.GetElement(ElementId)`, элементы RVT-связей через `RevitLinkInstance.GetLinkDocument().GetElement(linkedElementId)`;
- состояние статуса и комментария сохраняется в `%AppData%/TrueBIM/BimTools/clash-report/settings.json` по ключу model + clash id;
- кнопка `Выбрать` и двойной клик по строке выбирают найденные элементы текущей модели или доступные экземпляры RVT-связей;
- кнопка `Показать в 3D` создаёт или переиспользует служебный 3D-вид `BIM_Clash_Report_3D`, задаёт `SetSectionBox` вокруг найденного пересечения, переключает активный вид, выбирает найденные элементы и подсвечивает их overrides;
- отчёт можно сохранить в CSV UTF-8 with BOM.

Ограничения MVP:

- BCF-импорт пока не реализован;
- координаты `X/Y/Z` трактуются как внутренние координаты модели Revit, без отдельного выбора единиц;
- назначенные ответственные, история решений, marker geometry, solid-intersection и пакетные снимки пока вне MVP;
- настройки проверки, категории, допуски и поиск пересечений внутри Revit намеренно удалены;
- служебный 3D-вид переиспользуется, поэтому старые overrides внутри него могут оставаться до ручной очистки или следующей расширенной итерации.

Manual QA:

1. Установить свежий `TrueBIM-Setup.exe` или выполнить local deploy после закрытия Revit.
2. Открыть проект Revit.
3. Подготовить CSV или XML со списком коллизий и ElementId/координатами.
4. Вкладка `TrueBIM` -> панель `Координация` -> `Отчёт коллизий`.
5. Нажать `Добавить файл`, выбрать CSV и проверить список импортированных коллизий.
6. Нажать `Проверить`, чтобы обновить resolve ElementId в текущей модели.
7. Изменить статус/комментарий у нескольких строк, нажать `Сохранить`, закрыть и снова открыть окно, чтобы проверить восстановление JSON-состояния.
8. Выбрать строку с найденными ElementId, нажать `Выбрать` и проверить selection в Revit.
9. Нажать `Показать в 3D`.
10. Проверить служебный вид `BIM_Clash_Report_3D`, section box, выделение элементов и подсветку.
11. Выбрать строку только с координатами и проверить section box вокруг точки.
12. Повторить импорт XML и проверить тот же сценарий перехода.
11. Сохранить CSV-отчёт и проверить заголовки/значения в Excel или текстовом редакторе.

## Задача 0.9: Импорт Таблиц Спецификаций

Статус: частично готово.

Цель: создать безопасный импорт таблиц спецификаций из проектной документации без нарушения ограничений Revit `ViewSchedule`: сначала показать пользователю распознанную таблицу, затем для первого релиза создать визуальную таблицу на графическом виде, а BIM Schedule-запись оставить за явным dry-run и отдельным подтверждённым этапом.

Фактически реализовано:

- команда `ScheduleImportCommand` запускается кнопкой `Импорт таблиц` на панели `БИМ`;
- окно `ScheduleImportWindow` поддерживает выбор файла и drag & drop;
- рабочие входы первого релиза: `.pdf` через локальный `LocalCliPdfTableParser` и `.json` через промежуточную модель `ScheduleTableJsonReader`;
- `.dwg` показывается как плановый формат: пользователь получает предупреждение, что для первого релиза нужен экспорт DWG-листа в PDF или загрузка JSON-модели;
- предпросмотр таблицы строится в WPF `DataGrid`;
- вкладка `Колонки` показывает авто-сопоставление колонок с параметрами Revit;
- `Drafting Table Mode` создаёт визуальную таблицу через `ExternalEvent` и `DraftingTableService`: линии через `DetailCurve`, текст через `TextNote`, новый Drafting View создаётся при необходимости;
- `BIM Schedule Mode` пока не записывает модель, но выполняет dry-run: проверяет активную `ViewSchedule`, доступные поля, сопоставленные/несопоставленные колонки, выбранный ключ, пустые ключи и дубли;
- `Key Schedule Mode` оставлен экспериментальным и не создаёт строки в текущем срезе;
- изменения модели выполняются только внутри Revit `Transaction`.

Ограничения текущего среза:

- прямой DWG parser не реализован;
- BIM Schedule Mode не добавляет поля в `ScheduleDefinition`, не меняет порядок колонок и не записывает значения в элементы;
- Key Schedule Mode не создаёт key schedule/dummy rows;
- OCR для сканированных PDF вынесен за пределы MVP;
- ручная QA в Revit 2025 после local deploy ещё нужна перед релизной раздачей.

Manual QA:

1. Закрыть Revit перед local deploy или установкой новой сборки.
2. Открыть проект Revit 2025.
3. Вкладка `TrueBIM` -> панель `БИМ` -> `Импорт таблиц`.
4. Выбрать PDF с табличной спецификацией или JSON-модель таблицы и нажать `Распознать`.
5. Проверить вкладку `Предпросмотр`: строки и колонки должны соответствовать исходной таблице.
6. Проверить вкладку `Колонки`: для типовых колонок вроде `Марка`, `Длина`, `Количество` должны появиться предполагаемые типы данных и сопоставления.
7. В режиме `Drafting Table` нажать `Проверить`, затем `Создать в Revit`.
8. Проверить, что на активном графическом виде или новом Drafting View появилась визуальная таблица с линиями и текстом.
9. Открыть обычную `ViewSchedule`, повторить распознавание и выбрать режим `BIM Schedule`; кнопка создания должна оставаться недоступной, а `Проверить` должен показать dry-run сопоставления и диагностику ключа.
10. Выбрать `.dwg`: инструмент должен показать предупреждение про экспорт DWG в PDF/JSON и не пытаться создать таблицу.

## Задача 1: Ribbon И UI-Каркас

Статус: реализовано первым срезом.

Цель: сделать новые команды видимыми и открываемыми до добавления логики, которая меняет модель Revit.

Состав работ:

- Добавить панели `Проверка модели`, `Параметры` и `Администрирование`.
- Добавить четыре push button с иконками 32x32, tooltip, long description и отдельными классами `IExternalCommand`.
- Открывать общее WPF-окно-каркас примерно 900x650 для каждой команды.
- Не менять существующие панели `БИМ`, `КР`, `ЭОМ`, `СС` и их команды.
- Добавить smoke-тесты на имена кнопок, панели, command class, иконки и описания.

Приёмка:

- Вкладка `TrueBIM` по-прежнему загружается.
- Новые кнопки находятся на ожидаемых панелях.
- Каждая новая кнопка ведёт на отдельную Revit external command.
- Каждая кнопка открывает окно-каркас без изменений активной модели.
- Первый срез не создаёт, не удаляет и не меняет Revit-фильтры, параметры или рабочие наборы.

## Задача 2: Копирование Параметров

Статус: готово.

Цель: копировать выбранные изменяемые значения параметров с исходного элемента на выбранные элементы-получатели.

Планируемые файлы:

- `CopyParametersCommand.cs`
- `CopyParametersWindow`
- `ParameterCopyService`
- `ParameterCompatibilityService`
- `ElementSelectionService`
- `CopyParameterRow`
- `ParameterCopyResult`
- `ElementCopyReportRow`

Основное поведение:

- Использовать единственный предвыбранный элемент как исходный, если он есть; иначе попросить пользователя выбрать исходный элемент.
- Показывать копируемые параметры с именем, значением, storage type, источником instance/type, возможностью записи и предупреждениями.
- Давать выбрать элементы-получатели через Revit selection.
- Копировать значения внутри `Transaction`, учитывая `StorageType`, GUID shared parameter, built-in parameters, read-only флаги и совместимость типов.
- Показывать отчёт по скопированным значениям, пропускам и причинам пропуска.

Фактически реализовано:

- Команда `CopyParametersCommand` запускается из панели `Параметры`.
- Если выбран ровно один элемент, он используется как исходный; иначе команда просит выбрать исходный элемент.
- Окно `CopyParametersWindow` показывает доступные для копирования instance/type параметры, текущие значения, storage type и предупреждения для type/ElementId/опасных параметров.
- После выбора параметров Revit просит выбрать элементы-получатели.
- `ParameterCopyService` копирует значения внутри `Transaction` и ищет параметры-получатели по GUID shared parameter, `BuiltInParameter`, затем по имени.
- Поддержаны `String`, `Integer`, `Double`, `ElementId`; несовпадения типа, read-only параметры и отсутствующие параметры попадают в отчёт как пропуски.
- `Esc` при выборе элементов возвращает `Result.Cancelled`.

Manual QA:

1. Открыть модель Revit и вкладку `TrueBIM`.
2. На панели `Параметры` нажать `Копирование параметров`.
3. Выбрать исходный элемент или запустить команду с одним заранее выбранным элементом.
4. Отметить несколько безопасных параметров, например текстовые комментарии или марку.
5. Нажать `Выбрать получателей`, выбрать элементы на виде и завершить выбор.
6. Проверить TaskDialog-отчёт: количество выбранных параметров, получателей, скопированных и пропущенных значений.
7. Проверить свойства элементов-получателей и Undo Revit.

## Задача 3: Рабочие Наборы

Статус: готово.

Цель: создавать пользовательские worksets из проверенного CSV или Excel-шаблона.

Планируемые файлы:

- `CreateWorksetsCommand.cs`
- `CreateWorksetsWindow`
- `WorksetExcelReader`
- `WorksetCreationService`
- `WorksetValidationService`
- `WorksharingService`
- `WorksetImportRow`
- `WorksetCreateResult`
- `WorksetValidationIssue`

Основное поведение:

- Сначала поддержать CSV; `.xlsx` добавлять только если зависимость уже надёжно встроена в проект.
- Показывать предпросмотр нормализованных имён worksets до применения.
- Помечать строки как `Будет создан`, `Уже существует`, `Пустая строка`, `Недопустимое имя` или `Дубликат в файле`.
- Никогда не включать worksharing молча; предупреждать про undo history и требовать явное подтверждение.
- Создавать worksets внутри `Transaction` и показывать отчёт по созданным, пропущенным и ошибочным строкам.

Фактически реализовано:

- Команда `CreateWorksetsCommand` запускается из панели `Администрирование`.
- Окно `CreateWorksetsWindow` поддерживает выбор CSV-файла, повторную проверку и экспорт шаблона.
- `WorksetCsvReader` читает первый столбец CSV, пропускает заголовок `WorksetName` и нормализует двойные пробелы.
- `WorksetValidationService` помечает строки как `Будет создан`, `Уже существует`, `Пустая строка`, `Недопустимое имя` или `Дубликат в файле`.
- Если модель не workshared, инструмент показывает явное предупреждение и включает worksharing только после подтверждения.
- `WorksetCreationService` создаёт worksets внутри `Transaction`, повторно проверяет уникальность имени через Revit API и показывает итоговый отчёт.
- `.xlsx` и распределение элементов по рабочим наборам оставлены вне MVP.

Manual QA:

1. Открыть модель Revit и вкладку `TrueBIM`.
2. На панели `Администрирование` нажать `Рабочие наборы`.
3. Нажать `Экспортировать шаблон`, сохранить CSV и открыть его для проверки структуры.
4. Выбрать CSV с несколькими новыми именами, пустой строкой, дублем и уже существующим workset.
5. Проверить предпросмотр и статусы строк.
6. Нажать `Создать рабочие наборы`; если worksharing выключен, подтвердить включение только после сохранения модели.
7. Проверить TaskDialog-отчёт и список рабочих наборов Revit.

## Задача 4: Цвета По Параметрам

Статус: готово.

Цель: окрашивать элементы активного вида по значениям выбранного параметра через Revit view filters.

Планируемые файлы:

- `ColorByParameterCommand.cs`
- `ColorByParameterWindow`
- `ColorByParameterService`
- `ParameterValueReader`
- `ViewFilterService`
- `ColorPaletteService`
- `FilterNameBuilder`
- `BimCategoryItem`
- `BimParameterItem`
- `ColorRuleRow`
- `ColorApplyResult`

Основное поведение:

- Собирать категории из элементов, видимых на активном виде.
- Показывать параметры выбранных категорий с разделением по источнику и storage type.
- Показывать уникальные значения, включая `<Пусто / Не заполнено>`, с цветами и чекбоксом использования.
- Применять `ParameterFilterElement` и `OverrideGraphicSettings` только к активному виду.
- Использовать безопасные имена фильтров с префиксом `BIM_F_`.
- Ограничивать чрезмерное количество уникальных значений и предупреждать пользователя перед созданием большого числа фильтров.
- Очищать только фильтры активного вида, созданные этим инструментом, и не удалять пользовательские фильтры молча.

Фактически реализовано:

- Команда `ColorByParameterCommand` запускается из панели `Проверка модели`.
- Окно `ColorByParameterWindow` показывает категории элементов активного вида, список доступных instance/type параметров и уникальные значения выбранного параметра.
- Значения включают `<Пусто / Не заполнено>`; список ограничен первыми 40 уникальными значениями с предупреждением в статусе.
- `ViewFilterService` создаёт или обновляет `ParameterFilterElement` с безопасным именем `BIM_F_...` и применяет `OverrideGraphicSettings` только к активному виду.
- Для строковых, целых, числовых, `ElementId` и пустых значений создаются Revit filter rules; неподходящие сочетания попадают в отчёт как пропуски.
- Очистка снимает с активного вида только фильтры с префиксом `BIM_F_`, не удаляя сами элементы фильтров из проекта.
- Перед созданием более 25 фильтров показывается явное подтверждение.

Manual QA:

1. Открыть модель Revit и вид с элементами нескольких категорий.
2. На панели `Проверка модели` нажать `Цвета по параметрам`.
3. Оставить несколько категорий, выбрать безопасный параметр, например `Комментарии`, `Марка` или типовой параметр с ограниченным числом значений.
4. Проверить список уникальных значений и сгенерированные цвета.
5. Нажать `Применить к активному виду`, проверить TaskDialog-отчёт и появление фильтров `BIM_F_...` в Visibility/Graphics активного вида.
6. Проверить, что другие виды не изменились.
7. Нажать `Очистить раскраску` и убедиться, что фильтры TrueBIM сняты только с текущего вида, а пользовательские фильтры не тронуты.

## Задача 5: ParaManager MVP

Статус: готово.

Цель: импортировать shared parameters в проект из CSV или Excel с выбранным shared parameter file.

Планируемые файлы:

- `ParaManagerCommand.cs`
- `ParaManagerWindow`
- `SharedParameterFileService`
- `ProjectParameterBindingService`
- `ParameterExcelImportService`
- `CategoryResolveService`
- `ParameterExportService`
- `ParaManagerValidationService`
- `ParameterImportRow`
- `ParameterBindingPreviewRow`
- `ParameterImportResult`
- `SharedParameterDefinitionInfo`

Основное поведение:

- Начать с импорта project parameters из CSV/Excel.
- Требовать выбранный shared parameter `.txt`.
- До применения валидировать категории, типы данных, binding type, существующие definitions и конфликты привязки.
- Создавать или переиспользовать группы и definitions в shared parameter file.
- Привязывать instance/type параметры к найденным категориям внутри `Transaction`.
- Показывать отчёт по созданным definitions, привязанным параметрам, пропущенным строкам и ошибкам.

Фактически реализовано:

- Команда `ParaManagerCommand` запускается из панели `Параметры`.
- Окно `ParaManagerWindow` содержит вкладки `Параметры проекта`, `Импорт CSV` и `Отчёт`.
- Вкладка `Параметры проекта` читает реальные project parameter bindings из `Document.ParameterBindings` и показывает имя, тип, instance/type, категории, группу и GUID для shared parameters.
- Вкладка `Параметры проекта` экспортирует текущий список project parameters в CSV с колонками `ParameterName;BindingType;Categories;GroupUnder;DataType;IsShared;Guid`.
- Вкладка `Импорт CSV` поддерживает выбор CSV и shared parameter `.txt`, экспорт шаблона и предпросмотр строк.
- CSV MVP использует колонки `ParameterName;SharedGroup;BindingType;Categories;GroupUnder;DataType;Visible;UserModifiable;Description`.
- Поддержанные `DataType`: `Text`, `Integer`, `Number`, `YesNo`, `Length`, `Area`, `Volume`.
- Поддержанные `GroupUnder`: `Identity Data`, `Text`, `Dimensions`, `Data`, `General`, `Other`.
- `ParaManagerValidationService` до применения проверяет имя, дубли, binding type, категории, тип данных, группу свойств и boolean-флаги.
- `SharedParameterFileService` открывает выбранный `.txt`, создаёт группу и `ExternalDefinition`, если их нет.
- `ProjectParameterBindingService` создаёт instance/type binding, добавляет параметр через `BindingMap.Insert`, а для существующего параметра с тем же binding type расширяет категории через `ReInsert`.
- При конфликте instance/type строка не ломает модель, а попадает в отчёт как ошибка.
- Все изменения project parameters выполняются внутри `Transaction`.

Manual QA:

1. Открыть модель Revit и вкладку `TrueBIM`.
2. На панели `Параметры` нажать `ParaManager`.
3. На вкладке `Параметры проекта` нажать `Обновить список` и проверить текущие параметры.
4. Нажать `Экспорт CSV`, сохранить файл и проверить колонки `ParameterName`, `BindingType`, `Categories`, `GroupUnder`, `DataType`, `IsShared`, `Guid`.
5. На вкладке `Импорт CSV` нажать `Экспортировать шаблон`, сохранить CSV и открыть его для проверки структуры.
6. Выбрать существующий shared parameter `.txt`.
7. Выбрать CSV с новыми параметрами, существующим параметром, неверной категорией и неподдержанным типом данных.
8. Проверить предпросмотр: новые строки должны быть `Будет создан`, существующие - `Будет обновлён`, ошибочные - `Ошибка проверки`.
9. Нажать `Применить импорт`, подтвердить операцию, проверить TaskDialog-отчёт и появление параметров в Project Parameters Revit.
10. Проверить, что `.xlsx`, семейства, удаление параметров и редактор shared parameter file недоступны в этом MVP.

Отложенный объём:

- Массовое редактирование параметров семейств.
- Nested family parameters.
- Генерация новых GUID для существующих параметров.
- Merge нескольких shared parameter files.
- Удаление project parameters.
- Автоматическое изменение `.rfa` файлов.
- Импорт `.xlsx` без промежуточного CSV.

## Общие Правила

- Все изменения модели выполнять внутри Revit `Transaction`.
- При отмене пользовательского выбора возвращать `Result.Cancelled`, где используется Revit selection.
- Ошибки показывать через `TaskDialog` и писать в лог TrueBIM.
- Для автоматически созданных фильтров, настроек или definitions использовать префикс `BIM_`.
- Не удалять пользовательские фильтры, параметры или рабочие наборы без явного подтверждения.
