# Армирование по изополям: RoadMap до рабочего плагина

Обновлено: 17.07.2026.

## Цель

Превратить текущий экспериментальный модуль в управляемый инструмент для армирования ЖБ-плит по расчётным картам требуемой площади арматуры. Пользователь должен загрузить комплект изополей, проверить распознанные зоны, связать их с плитой Revit, настроить правила перевода `см²/м` в реальные стержни и получить проверяемую раскладку с возможностью безопасного повторного запуска.

Текущий производственный контур ограничивается planar-host: простыми горизонтальными плитами и прямыми базовыми стенами. Наклонные плиты, криволинейные/составные стены и сложные пространственные поверхности идут отдельными этапами после runtime-проверки текущей геометрии.

## Исходные материалы

Для проектирования сценария просмотрены четыре расчётные карты ПК ЛИРА:

- `...As1X1.png` — карта `As1X`;
- `...As2X1.png` — карта `As2X`;
- `...As3Y1.png` — карта `As3Y`;
- `...As4Y1.png` — карта `As4Y`.

Рабочая гипотеза для UX: это четыре независимых слоя армирования по направлениям X/Y и граням элемента. Точное соответствие `As1...As4` нижней/верхней арматуре должно быть подтверждено на первом пользовательском прогоне; до этого автоматическая запись обязана явно показывать выбранное назначение каждого слоя.

Файл `АрмированиеПоИзополям_rvt23.rvt` найден как отдельный файл, а не каталог. Без запуска Revit через `BasicFileInfo` подтверждено: формат Revit 2023, язык сохранения Russian, модель workshared/central. Внутренние виды, плиты, отверстия и уже созданная арматура статически не исследовались — они входят в ручную проверку пользователя.

## Текущее состояние

Уже работает на уровне безопасного технического MVP:

- отдельный модуль и кнопка в ленте TrueBIM;
- загрузка готового JSON-контракта зон;
- встроенное offline-распознавание шкалы и цветных hotspot-зон; внешний CLI worker остаётся опциональным override через окружение;
- комплект из четырёх изображений с ролями `As1X`, `As2X`, `As3Y`, `As4Y`, мультивыбором, миниатюрами, ручной коррекцией и проверкой pixel size;
- проверка роли по растровому заголовку ПК ЛИРА с confidence, объяснением источника назначения и блокировкой конфликтов имени/заголовка;
- извлечение `5–8` цветов шкалы на предоставленных картах, фильтрация сетки КЭ, объединение соседних цветных ячеек и построение консервативных convex-hull контуров с confidence;
- ручная таблица проверки результата: исключение зоны, смена класса по легенде и контролируемое объединение зон одного слоя/класса в общий convex hull;
- последовательная обработка четырёх карт с сохранением роли в контурах и диагностике;
- явное назначение верх/низ для каждого расчётного слоя с блокировкой записи при неоднозначности;
- сохранение и повторная загрузка manifest с относительными путями, размерами и SHA-256 исходников;
- preview контуров в окне и управляемые линии на активном 2D-виде;
- выбор одной прямой базовой стены или горизонтальной плиты; читаются локальные оси, внешний контур и отверстия наружной плоскости стены либо верхней грани плиты;
- трёхточечная привязка pixel-space к локальным координатам planar-host: первые две точки задают масштаб/поворот, третья независимо проверяет результат с допуском 50 мм;
- фактическое managed-отсечение зон по внешнему контуру и отверстиям host с площадью сохранённой части, несколькими регионами и блокировкой полностью потерянных зон;
- итоговый overlay отсечённых зон, контура, отверстий и трёх контрольных точек; частичное отсечение показано предупреждением, полностью потерянная зона — красным;
- сохранение проверенного профиля по ключу `документ + активный вид + ElementId host` в пользовательские настройки TrueBIM;
- ручная калибровка координат;
- инженерный расчёт по верхней границе диапазона `см²/м`, разбор сочетаний `d10s200+...` и проверка принятой площади;
- явный выбор `дополнительное усиление поверх базовой сетки / полное сочетание внутри зон`;
- read-only preview количества и линий стержней внутри отсечённых регионов;
- создание отдельных стержней по clipped-геометрии с защитным слоем, отступом от границ, минимальной длиной и подтверждением;
- строгий подбор `RebarBarType` по фактическому диаметру и стабильные комментарии с layer/face/zone id;
- diff повторного запуска `добавить / обновить / удалить / без изменений` с проверкой сигнатуры и фактической геометрии;
- отдельное сравнение с моделью и таблица зон с поиском, общими счётчиками и фильтрами `слой / статус / диаметр / шаг / confidence`;
- ручная настройка выбранной строки перед сравнением: замена распознанного сочетания, исключение зоны из раскладки и сброс одной либо всех настроек;
- инженерное объединение касающихся зон с одинаковым правилом без convex-hull расширения: общий polygon union, обратимое разъединение и детерминированный stable id от состава исходных зон;
- экспорт парного JSON/CSV-отчёта по зонам и итогам слоёв с hashes исходников, версией worker, привязкой, инженерными настройками, fingerprint профиля правил и состоянием diff;
- автоматический QA инженерной раскладки: дефицит площади, пересечения зон одного слоя и выход итоговой геометрии за host блокируют работу; неполное покрытие, clipping и исходные зоны вне host требуют явного решения пользователя;
- экспорт QA fingerprint, классифицированных ошибок/предупреждений и покрытия каждого слоя в JSON/CSV;
- защита от устаревшего diff: непосредственно перед применением модель читается повторно, а несовпадение блокирует запись и обновляет таблицу;
- идемпотентное применение по выбранному host: модуль обновляет только свои элементы со стабильным id и не трогает ручную арматуру;
- логирование и unit-тесты базовых преобразований.

Что пока не позволяет считать плагин производственным:

- числовые границы шкалы (`см²/м`) и сочетания диаметр/шаг распознаются для текущего шаблона ПК ЛИРА; зоны получают диапазон, а карточка показывает подписи его границ;
- узкий классификатор заголовка рассчитан на текущий шаблон экспорта ПК ЛИРА; при другом шрифте или масштабе роль определяется по имени либо вручную;
- легенда с RGB, числовыми диапазонами и подписями сочетаний пока остаётся внутренней managed-моделью и не сериализуется в JSON-контракт `1.0`;
- режим `полное сочетание` создаёт арматуру только внутри распознанных зон и пока не достраивает фоновую базовую сетку по всей плите;
- создаются отдельные стержни, а не `Rebar Set`/`Area Reinforcement`; объединение совместимых зон формирует непрерывную геометрию, но не группирует созданные Rebar в системный набор;
- защитный слой, отступ и минимальная длина учитываются, но нахлёсты, анкеровка и конструктивная арматура ещё не реализованы;
- инженерное объединение допускает только касающиеся или пересекающиеся зоны с одинаковыми слоем, гранью, направлением и сочетанием; разрывы не заполняются автоматически.

## Целевой пользовательский сценарий

1. Выбрать плиту или набор плит и сохранить локальные направления X/Y.
2. Загрузить четыре карты `As1X`, `As2X`, `As3Y`, `As4Y`; плагин автоматически группирует их в один расчётный комплект и показывает отсутствующие/дублирующиеся слои.
3. Распознать легенду и зоны, проверить наложение на плиту, вручную исправить назначение слоя или границы спорной зоны.
4. Настроить базовое армирование, таблицу допустимых диаметров/шагов, защитный слой и правила усиления; увидеть расчёт и конфликты.
5. Перед сравнением проверить строки раскладки, при необходимости заменить сочетание, исключить конкретную зону либо объединить совместимые соседние зоны и убедиться, что линии и количество пересчитаны.
6. Создать армирование только после финального подтверждения, затем получить отчёт: слой, зона, требуемая/принятая площадь, диаметр, шаг, количество и диагностические предупреждения.

## RoadMap

### P0. UX и честные состояния текущего MVP — выполнено в текущем срезе

- Исправить фактический размер rasterized-иконок ленты: `LargeImage` должен быть настоящим 32×32, а не геометрией 16×16 с другим ключом кэша.
- Увеличить общие WPF-токены иконок и централизовать размеры ribbon small/large.
- Перестроить окно в последовательность `источник → зоны → host → правила → создание`.
- Объединить выбор источника и запуск обработки в одном шаге.
- Отключать недоступные действия до выполнения предпосылок и показывать причину в tooltip даже для disabled-кнопок.
- Использовать встроенный offline-runner как рабочий fallback, если внешний CLI worker не настроен; честно показывать ограничения результата.
- Заменить статичный блок «Будущие шаги» живым индикатором готовности `0...4`.
- Спрятать ручную калибровку в раскрываемые дополнительные настройки.
- В UI называть текущую запись «пробным армированием» и отдельно предупреждать, что это не производственная раскладка.

Критерий готовности: новый пользователь понимает следующий шаг без вызова ошибочного `TaskDialog`; опасная кнопка недоступна до валидных правил.

### P1. Контракт комплекта изополей — в работе

- [x] Добавить модель `IsoFieldSourceSet` с четырьмя обязательными ролями: `As1X`, `As2X`, `As3Y`, `As4Y`.
- [x] Поддержать мультивыбор изображений и автоматическое определение роли по имени.
- [x] Добавить ручное переназначение роли, миниатюру каждой карты и проверку одинакового pixel size.
- [x] Обрабатывать четыре карты последовательно и сохранять роль в объединённом результате.
- [x] Дополнить определение роли чтением заголовка внутри изображения; при конфликте с именем файла требовать ручное подтверждение.
- [x] Сохранять manifest комплекта рядом с исходниками, чтобы повторная загрузка была воспроизводимой; проверять размеры и SHA-256.
- [x] Показывать явную таблицу `расчётный слой → X/Y → верх/низ` и блокировать запись до однозначного назначения.
- [ ] После первого пользовательского прогона подтвердить инженерское соответствие `As1...As4` верхней/нижней грани и решить, допустимы ли default-значения.

Критерий готовности: неполный или неоднозначный комплект нельзя отправить на создание; пользователь видит конкретную проблему по каждому файлу.

### P2. Поставляемое распознавание легенды и зон

- [x] Добавить встроенный managed-распознаватель без сети, внешнего процесса и новых native DLL; CLI оставить опциональным override.
- [x] Находить горизонтальную цветовую шкалу отдельно от расчётной области и извлекать её RGB-уровни.
- [x] Фильтровать тонкую сетку КЭ, объединять соседние цветные ячейки, строить convex-hull зоны с confidence и сохранять исходный pixel-space.
- [x] Показывать в окне отдельную цветовую шкалу для каждого слоя и честную диагностику результата.
- [x] Извлекать числовые диапазоны `As` в `см²/м`, проверять монотонность границ и переносить диапазон в подпись зоны.
- [x] Распознавать подписи сочетаний вида `d10s200+d14s200`, проверять полный упорядоченный набор и связывать их с границами диапазонов шкалы.
- [x] Дать пользователю экран коррекции: исключить зону, изменить класс по легенде, объединить выбранные зоны одного слоя/класса; после правки сбрасывать правила и явно обновлять preview.
- Добавить эталонные fixtures из четырёх предоставленных PNG и регрессионные метрики.

Критерий готовности: все четыре эталонные карты обрабатываются офлайн одинаково при повторном запуске; низкий confidence не проходит без ручного подтверждения.

### P3. Геометрическая привязка к плите — техническая реализация выполнена, нужен smoke в Revit

- [x] Выбирать конкретную горизонтальную плиту до записи и строить локальную систему координат верхней грани.
- [x] Поддержать привязку по двум опорным точкам с расчётом единого масштаба и поворота.
- [x] Показывать итоговый overlay зон поверх внешнего контура и отверстий плиты до расчёта правил.
- [x] Обрезать зоны по границе плиты, вычитать отверстия, сохранять отдельные регионы и долю оставшейся площади.
- [x] Проверять поворот, зеркальность, вырожденные пары точек и выход зон/контрольных точек за host.
- [x] Независимо проверять привязку по третьей неколлинеарной точке с допуском 50 мм.
- [x] Сохранять и повторно проверять профиль привязки на том же документе, виде и плите.

Критерий готовности: контрольные зоны из PNG попадают в ожидаемые места файла `АрмированиеПоИзополям_rvt23.rvt`, а несовместимая привязка блокирует создание.

### P4. Инженерные правила и производственная раскладка плит — P4.1 реализован технически, нужен smoke в Revit

- [x] Зафиксировать единицы исходных значений (`см²/м`), принимать верхнюю границу диапазона зоны и считать площадь сочетания по диаметру/шагу.
- [x] Явно выбирать расчёт только дополнительного усиления поверх первой базовой сетки из подписи либо полное сочетание внутри зон.
- [x] Подбирать распознанное сочетание диаметр/шаг с проверкой, что принятая площадь не меньше требуемой.
- [x] Разделять раскладку по направлениям X/Y и верхней/нижней граням; X расположен ближе к соответствующей грани, Y — глубже с зазором 5 мм.
- [x] Учитывать защитный слой, отступ от контура/отверстий, минимальную длину и лимит количества стержней.
- [x] Отсекать каждую линию по внешнему контуру clipped-региона и отверстиям; показывать линии и расчётное количество до записи.
- [ ] Создавать фоновую базовую сетку по всей допустимой области плиты для режима полного армирования.
- [ ] Делить/объединять соседние зоны и превращать отдельные линии в пригодные для выпуска `Rebar Set`/`Area Reinforcement`.
- [x] Помечать элементы стабильными `TrueBIM`-идентификаторами, исходным zone id, слоем, гранью и сочетанием.
- [ ] Добавить нахлёсты, анкеровку и конструктивные ограничения после подтверждения проектных правил.

Критерий готовности: созданная площадь не меньше требуемой, каждый стержень находится внутри допустимой геометрии host, результат разделён на четыре понятных слоя.

### P5. Preview, редактирование и повторный запуск — P5.3b реализован технически, нужен smoke в Revit

- [x] Показывать будущие линии раскладки в окне без постоянных `DetailCurve`; временный Revit-preview очищается отдельно.
- [x] Показать таблицу зон с поиском, итоговыми счётчиками и фильтрами `слой`, `статус`, `диаметр`, `шаг`, `confidence`.
- [x] Разрешить для выбранной зоны заменить распознанное сочетание или исключить её из раскладки; проверять принятую площадь, пересчитывать preview и сбрасывать устаревший diff.
- [x] Сохранять исключённую строку видимой со статусом `Исключена`, удалять ранее созданные модулем стержни этой зоны при следующем применении и блокировать полностью пустую раскладку.
- [x] Дать сброс настройки одной зоны и всех ручных настроек с возвратом к рассчитанным правилам.
- [x] Объединять касающиеся инженерные зоны с одинаковым правилом непосредственно перед записью через точный polygon union, сохраняя отверстия и не заполняя разрывы convex hull.
- [x] Формировать merged zone id как детерминированный hash отсортированных исходных zone id; порядок выделения не меняет stable id, а разъединение возвращает исходные id.
- [x] Поддержать мультивыбор `Ctrl/Shift`, явные команды объединения/разъединения, минимальный confidence группы, поиск по исходным id и общий сброс ручной конфигурации.
- [x] Отделить `Сравнить с моделью` от `Применить изменения` и показывать `добавить / обновить / удалить / без изменений` до доступности записи.
- [x] Сравнивать не только сохранённую сигнатуру, но и фактическую линию/диаметр; изменённые и старые элементы P4.1 пересоздавать контролируемо.
- [x] Перед применением повторно проверять fingerprint diff; при изменении модели блокировать запись и требовать просмотра обновлённой таблицы.
- [x] Обновлять только принадлежащие модулю элементы выбранного host со стабильным id и не трогать ручную арматуру.
- [x] Применять добавление, обновление и удаление одной транзакцией Undo; при отсутствии изменений не открывать транзакцию.

Критерий готовности: повторный запуск с теми же данными не создаёт дублей; пользователь понимает состав изменений до подтверждения.

### P6. Отчёт, диагностика и контроль качества — P6.3 реализован технически, нужен smoke в Revit

- [x] Экспортировать рядом JSON и CSV UTF-8 по каждой зоне и итогам слоя; фиксировать, выполнено ли сравнение с моделью.
- [x] Сохранять пути и SHA-256 исходников, размеры изображений, manifest, имя/версию worker, версию плагина, калибровку или привязку плиты, инженерные настройки и fingerprint профиля правил.
- [x] Отдельно показывать блокирующие ошибки и предупреждения, допустимые по решению пользователя; подтверждение предупреждений действует только для текущего preview.
- [x] Проверять покрытие четырёх слоёв, пересечения зон одного слоя, выход итоговой геометрии за host и несоответствие требуемой/принятой площади; сохранять результат и fingerprint в отчёт.
- [x] Сформировать короткий итог после создания со счётчиками применения, явным обновлением последнего отчёта и быстрым открытием JSON/лога.

После успешной записи блок результата показывает `добавлено / обновлено / удалено / без изменений`, host и время. Отчёт schema `1.2` сохраняет итог применения и element ids; если JSON/CSV были экспортированы до записи, окно явно помечает их устаревшими и предлагает обновить по тому же пути только по команде пользователя.

Критерий готовности: по результату можно восстановить, из какого комплекта и по каким правилам создан каждый набор.

### P7. Стены, сложная геометрия и выпуск — P7.2 прямые стены реализованы технически

- [x] До расчёта классифицировать host: горизонтальная плита, прямая базовая стена или неподдерживаемая геометрия; использовать один preflight-контракт в UI и перед транзакцией.
- [x] Оставлять неподдерживаемый host видимым для диагностики, показывать стабильный код/причину и одинаково блокировать расчёт, diff и запись.
- [x] Перевести прямые базовые стены с одной непрерывной наружной planar-гранью в инженерный сценарий: геометрия с отверстиями, трёхточечная привязка, внутренняя/наружная грань, QA, diff и идемпотентное применение.
- [ ] Отдельно добавить curved/stacked/curtain walls после подтверждения инженерных правил на прямой стене.
- [ ] Добавить наклонные плиты, сложные контуры и compound structure отдельными проверяемыми срезами.
- [ ] Провести smoke на Revit 2023 с предоставленной моделью и регрессию поддерживаемой матрицы TrueBIM.
- [x] Обновить встроенную методичку, manual QA и installer payload для P7.1/P7.2.
- [ ] Выпускать production-режим только после подтверждения инженером результатов на эталонной модели.

P7.1 переносит отказ из поздней операции записи в понятный preflight. P7.2 добавляет engineering-профиль `StraightBasicWall`: локальная X идёт вдоль стены, локальная Y — вверх, `Bottom` в сохранённом контракте означает внутреннюю грань, `Top` — наружную. Раскладка использует фактическую толщину стены, защитный слой, отверстия и стабильные id; перед записью геометрия host читается повторно. Стены с несколькими раздельными наружными planar-гранями блокируются вместо частичной обработки. Криволинейные, составные и витражные стены, а также плиты без распознанной горизонтальной верхней грани по-прежнему блокируются.

## Definition of Done для первого производственного релиза

- Четыре карты загружаются как один комплект и имеют однозначное назначение.
- Легенда и зоны воспроизводимо распознаются офлайн, спорные места видны пользователю.
- Overlay совпадает с выбранной плитой и корректно учитывает внешний контур и отверстия.
- Правила явно показывают требуемую и принятую площадь арматуры.
- Создаются полноценные наборы по X/Y и верх/низ, а не одиночные пробные стержни.
- Повторный запуск не создаёт дублей и предварительно показывает diff.
- Все изменения отменяются через Undo, ручная арматура не затрагивается.
- Сохраняется отчёт с источниками, правилами, предупреждениями и созданными element ids.
- Unit/integration-тесты зелёные; ручной smoke пройден на `АрмированиеПоИзополям_rvt23.rvt`.

## Данные, которые нужно подтвердить после первого пользовательского прогона

- точное назначение `As1X`, `As2X`, `As3Y`, `As4Y` относительно верхней/нижней граней;
- создаём полную расчётную арматуру или добавку к заранее заданной базовой сетке;
- разрешённые диаметры, минимальный/максимальный шаг и правило округления;
- требуемые защитные слои, нахлёсты, анкеровка и правила обработки отверстий;
- нужно ли группировать результат в `Area Reinforcement`, `Path Reinforcement`, обычные `Rebar Set` или сохранять несколько режимов.
